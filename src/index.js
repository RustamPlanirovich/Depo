import React from 'react';
import ReactDOM from 'react-dom/client';
import DepositTracker from './main';

const root = ReactDOM.createRoot(document.getElementById('app'));
root.render(
  <React.StrictMode>
    <DepositTracker />
  </React.StrictMode>
); 